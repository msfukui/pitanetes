#!/bin/bash

# worker node's hostname
hosts=(
  spotty
  painty
)

bundle config --local path vendor/bundle
bundle install

# Setup k8s worker nodes
for node in "${hosts[@]}" ; do
  echo "Setup node: [${node}]"
  bundle exec itamae ssh -u ubuntu -h ${node} -j nodes/${node}.json roles/default.rb
done
